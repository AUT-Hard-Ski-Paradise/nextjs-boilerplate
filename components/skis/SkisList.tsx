import {Text} from "@radix-ui/themes";
import {useCallback, useEffect, useState} from "react";
import axios from "axios";
import {HealthResponse} from "@/types";

interface Props{

}

export const SkisList: React.FC<Props> = (props) => {

    return (
        <Text>Asd</Text>
    )
}
